# --- API ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
python-multipart>=0.0.12        # file uploads

# --- Config / validation ---
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0

# --- Database ---
sqlalchemy>=2.0,<3.0

# --- LLM (swap providers with LLM_PROVIDER in .env) ---
langchain-core>=0.3,<1.0
langchain-google-genai>=2.0,<3.0
langchain-openai>=0.2,<1.0
tenacity>=9.0,<10.0             # retry on rate limits

# --- Reading resume files ---
pdfplumber>=0.11,<1.0
python-docx>=1.1,<2.0
striprtf>=0.0.26

# --- Tests ---
pytest>=8.3,<9.0
httpx>=0.27,<1.0                # FastAPI TestClient
